{
  "name": "midi-player-js",
  "version": "1.0.0",
  "description": "A javascript library to play MIDI files",
  "main": "build/index.js",
  "scripts": {
    "test": "mocha"
  },
  "devDependencies": {
    "babel-cli": "^6.14.0",
    "babel-preset-es2015": "^6.14.0",
    "mocha": "~2.4.5",
    "uglify-js": "^2.7.3"
  },
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "mocha",
    "build": "babel src --out-file build/index.js && uglifyjs build/index.js -m > build/index.min.js && ls -l build/"
  },
  "keywords": [
    "midi",
    "parser"
  ],
  "author": "Garrett Grimm",
  "license": "MIT",
    "babel": {
    "presets": [
      "es2015"
    ],
    "sourceMaps": "inline"
  }
}
